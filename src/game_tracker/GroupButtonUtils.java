/*
 * This is just a class that is going to be used when I want to pretty the code up.
 * 
 * 
 * */

/*package game_tracker;

import java.util.Enumeration;

import javax.swing.AbstractButton;
import javax.swing.ButtonGroup;

public class GroupButtonUtils 
{
	 public Boolean getSelectedButton(ButtonGroup buttonGroup)
	 {
	        for (Enumeration<AbstractButton> buttons = buttonGroup.getElements(); buttons.hasMoreElements();) 
	        {
	            AbstractButton button = buttons.nextElement();

	            if (button.isSelected() && LoadDriver.active == true) 
	            {
	            	LoadDriver.active = false;
	            	button.setEnabled(true);
	            }
	            else
	            {
	            	LoadDriver.active = true;
	            	//button.setEnabled(false);
	            }
	            	
	        }

	        return false;
	   }
}
*/